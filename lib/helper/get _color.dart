import 'package:flutter/material.dart';

class GetColor {
  dynamic getColor() {
    List<Color> colorsList = [Colors.red, Colors.blue];

    for (int i = 0; i < colorsList.length;) {
      return colorsList[i];
    }
    // if(i==colorsList.length)
    // {
    //   i=0;
    // }
  }
}
